version: "3.8"
services:
  app:
    build: ./app/
    ports:
     - 8080:8080
    networks:
        - course_reg_nw
  db:
    build: ./database/
    networks:
        - course_reg_nw
networks:
   course_reg_nw:
      driver: bridge